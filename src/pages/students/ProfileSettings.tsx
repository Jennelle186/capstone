import ProfileSettings from "@/components/student/ProfileSettings/ProfileSettings";

export default function ProfileSettingsPage() {
  return (
    // Page wrapper keeps StudentLayout padding and animations intact.
    <ProfileSettings />
  );
}
