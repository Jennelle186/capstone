from fastapi import APIRouter

# Access control domain router.
# Permission and policy endpoints will be added here as admin access-control APIs are built.
router = APIRouter(prefix="/access-control")
