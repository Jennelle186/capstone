# Clerk secret key for backend token verification.
CLERK_SECRET_KEY=sk_test_xxx
# Optional: JWT public key from Clerk Dashboard -> JWT Templates.
# If set, backend can verify tokens without fetching Clerk JWKS over network.
CLERK_JWT_KEY=-----BEGIN PUBLIC KEY-----\n...\n-----END PUBLIC KEY-----
