This folder contains Alembic migrations.

Common commands (run from `backend/` with the venv activated):
  alembic upgrade head
  alembic revision --autogenerate -m "your message"
  alembic stamp head  # use this if your DB already exists and matches the baseline
